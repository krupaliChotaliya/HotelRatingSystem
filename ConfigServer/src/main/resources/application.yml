server:
  port: 8085
spring:
  application:
    name: Config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/krupaliChotaliya/microservice-configuration
          clone-on-start: true

